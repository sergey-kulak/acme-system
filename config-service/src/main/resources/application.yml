server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sergey-kulak/acme-config-repo

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7761/eureka/